class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int num=nums.size();
        for(int i=1;i<num;i++) nums[0]^=num[i];
        return num[0];
    }
};//不需要额外空间的方法，就往位运算上想
/*位运算总结
与（&）：清零、取一个数的指定位（位置全为1，其余为0）
或（|）：某些位设置为1
非（~）
异或（^):反转指定位、反身性为0
左移（<<)右移（>>)
